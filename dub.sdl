name "pythonapi"
authors "Atila Neves"
license "boost"

targetType "library"
targetPath "bin"

#preGenerateCommands "make -C $PACKAGE_DIR source/python/raw.d"

configuration "library" {

}

configuration "unittest" {
    targetType "executable"
    targetName "ut"

    mainSourceFile "tests/main.d"
    sourcePaths "tests"
    importPaths "tests"

    dflags "-preview=dip1000" "-preview=dip1008"

    dependency "unit-threaded" version="*"
}
